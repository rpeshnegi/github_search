import React from 'react';

type Props = {}

const Navigation: React.FC<Props> = function Navigation() {
  return (
    <header className="App-header">
      <h1 className="App-title">Github Search</h1>
    </header>
  );
};

export default Navigation;
